<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aasthaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AasthaApp">
        <activity android:name=".chatDetailActivityCp"></activity>
        <activity android:name=".MainActivitylgbt" />
        <activity android:name=".SignInActivityCP" />
        <activity android:name=".MainActivityCP" />
        <activity android:name=".signUPActivityCP" />
        <activity android:name=".chatDetailActivity" />
        <activity android:name=".aboutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".signUPActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>